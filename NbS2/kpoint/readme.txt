To converge total energy by varying the kpoint grid:
	Run run.sh (edit the code to coincide with your scf input file)
		example:
			./run.sh
		
		Note: Be sure that your run.sh has execution permission. If not, use the following command and re-run it:
			sudo chmod +x run.sh
			
